describe('ViewPorts Sizes', () => {
    it('Test the veiwports', () => {
        cy.visit('http://www.webdriveruniversity.com');
        // cy.viewport(550,750)
        // cy.get('#datepicker').invoke('removeAttr', 'target').click({force: true});
    });
});